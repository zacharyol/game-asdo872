<?xml version="1.0" encoding="UTF-8" ?>
<Module>
  <ModulePrefs title="Scritchy Scratchy" />
  <Content type="html"><![CDATA[
<!DOCTYPE html>
<html lang="en-us">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Scritchy Scratchy</title>
    <style>
      html, body {
        width: 100%;
        height: 100%;
        margin: 0;
        padding: 0;
        overflow: hidden;
        background-color: #000;
      }

      #unity-canvas {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background-color: #000;
        display: block;
      }

      #loading-screen {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: #000;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        color: white;
        font-family: Arial, sans-serif;
        font-size: 18px;
        z-index: 10;
      }

      #loading-text {
        margin-bottom: 15px;
      }

      #progress-container {
        width: 40%;
        height: 10px;
        background: #222;
        border-radius: 5px;
        overflow: hidden;
      }

      #progress-bar {
        height: 100%;
        width: 0%;
        background: #09f;
        transition: width 0.2s linear;
      }
    </style>
  </head>
  <body>
    <div id="loading-screen">
      <div id="loading-text">Loading...</div>
      <div id="progress-container">
        <div id="progress-bar"></div>
      </div>
    </div>

    <canvas id="unity-canvas"></canvas>

    <script src="https://cdn.jsdelivr.net/gh/zacharyol/game-asdo872@main/game/Build/WebGL.loader.js"></script>

    <script>
      if (/iPhone|iPad|iPod|Android/i.test(navigator.userAgent)) {
        const meta = document.createElement("meta");
        meta.name = "viewport";
        meta.content = "width=device-width, height=device-height, initial-scale=1.0, user-scalable=no, shrink-to-fit=yes";
        document.getElementsByTagName("head")[0].appendChild(meta);
      }

      const loadingScreen = document.getElementById("loading-screen");
      const progressBar = document.getElementById("progress-bar");
      const loadingText = document.getElementById("loading-text");

      createUnityInstance(document.querySelector("#unity-canvas"), {
        dataUrl: "https://cdn.jsdelivr.net/gh/zacharyol/game-asdo872@main/game/Build/WebGL.data.br",
        frameworkUrl: "https://cdn.jsdelivr.net/gh/zacharyol/game-asdo872@main/game/Build/WebGL.framework.js.br",
        codeUrl: "https://cdn.jsdelivr.net/gh/zacharyol/game-asdo872@main/game/Build/WebGL.wasm.br",
        streamingAssetsUrl: "https://cdn.jsdelivr.net/gh/zacharyol/game-asdo872@main/game/StreamingAssets",
        companyName: "Lunch Money Games",
        productName: "Scritchy Scratchy",
        productVersion: "0.8"
      }, function(progress) {
        progressBar.style.width = (progress * 100) + "%";
      }).then(function() {
        loadingScreen.style.display = "none";
      }).catch(function(err) {
        loadingText.textContent = "Error loading game: " + err;
        loadingText.style.color = "red";
      });
    </script>

    <style>
      #ad-container {
        position: fixed;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: min(728px, calc(100% - 20px));
        height: 90px;
        background: rgba(0, 0, 0, 0.90);
        display: none;
        z-index: 99999;
        overflow: hidden;
        box-shadow: 0 -2px 12px rgba(0, 0, 0, 0.45);
        box-sizing: border-box;
        transition: transform 0.5s ease-in-out;
      }

      #ad-container.hidden {
        transform: translate(-50%, 100%);
      }

      #ad-iframe {
        position: absolute;
        top: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 745px;
        height: 90px;
        border: 0;
        display: block;
        overflow: hidden;
        pointer-events: auto;
        box-sizing: content-box;
        -ms-overflow-style: none;
        scrollbar-width: none;
      }

      #ad-iframe::-webkit-scrollbar {
        display: none;
        width: 0;
        height: 0;
      }

      #close-ad {
        position: absolute;
        top: 6px;
        right: 8px;
        background: #ff4d4d;
        color: #fff;
        border: none;
        padding: 5px 9px;
        font-size: 13px;
        border-radius: 4px;
        cursor: not-allowed;
        opacity: 0.72;
        z-index: 100000;
        display: flex;
        align-items: center;
      }

      #close-ad.enabled {
        cursor: pointer;
        opacity: 1;
      }

      #close-ad::before {
        content: '↓';
        margin-right: 4px;
      }

      #ad-right-mask {
        position: absolute;
        top: 0;
        right: 0;
        width: 12px;
        height: 100%;
        pointer-events: none;
        background: linear-gradient(to right, rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.9));
        z-index: 99999;
      }

      @media (max-width: 440px) {
        #ad-container {
          width: calc(100% - 12px);
          left: 50%;
          transform: translateX(-50%);
        }

        #ad-iframe {
          width: 728px;
        }
      }
    </style>

    <div id="ad-container" aria-hidden="true" role="dialog" aria-label="Advertisement">
      <iframe
        id="ad-iframe"
        src="https://script.google.com/macros/s/AKfycbzgdj27cuXPajaCcNKtN6W6z08sF2N-4kSjtutgUvLYFnkB-tdYeJ0tdAEZ2yPPfCBr/exec"
        width="768"
        height="95"
        scrolling="no"
        frameborder="0"
        sandbox="allow-scripts allow-popups allow-same-origin"
      ></iframe>
      <button id="close-ad" disabled>Close (12)</button>
      <div id="ad-right-mask"></div>
    </div>

    <script>
      (function () {
        const showDelay = 2000;
        const countdownStart = 12;
        const reappearDelay = 150000;
        const adContainer = document.getElementById("ad-container");
        const closeBtn = document.getElementById("close-ad");

        function showAd() {
          adContainer.style.display = "block";
          adContainer.classList.remove("hidden");
          adContainer.setAttribute("aria-hidden", "false");

          let timeLeft = countdownStart;
          closeBtn.textContent = "Close (" + timeLeft + ")";
          closeBtn.disabled = true;
          closeBtn.classList.remove("enabled");

          const t = setInterval(() => {
            timeLeft--;
            if (timeLeft > 0) {
              closeBtn.textContent = "Close (" + timeLeft + ")";
            } else {
              clearInterval(t);
              closeBtn.disabled = false;
              closeBtn.classList.add("enabled");
              closeBtn.textContent = "Close ↓";
            }
          }, 1000);
        }

        setTimeout(showAd, showDelay);

        closeBtn.addEventListener("click", () => {
          if (closeBtn.disabled) return;
          adContainer.classList.add("hidden");
          adContainer.setAttribute("aria-hidden", "true");
          setTimeout(showAd, reappearDelay);
        });
      })();
    </script>
  </body>
</html>
  ]]></Content>
</Module>